# 配置与部署

## 1. 配置管理

### 配置文件 (.env)

```env
# 应用配置
APP_NAME=ZeroLife
APP_ENV=production
APP_PORT=8080
APP_DEBUG=false
APP_URL=https://api.example.com
APP_TIMEZONE=Asia/Shanghai

# 数据库
DB_DRIVER=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=zero_life
DB_USERNAME=root
DB_PASSWORD=secret
DB_MAX_IDLE_CONNS=10
DB_MAX_OPEN_CONNS=100
DB_CONN_MAX_LIFETIME=3600

# Redis
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# JWT
JWT_SECRET=your-secret-key-at-least-32-chars
JWT_EXPIRATION=24h
JWT_REFRESH_EXPIRATION=168h

# CORS
CORS_ALLOWED_ORIGINS=https://app.example.com,http://localhost:5173

# 日志
LOG_LEVEL=info
LOG_FORMAT=json
LOG_OUTPUT=stdout

# 文件上传
UPLOAD_MAX_SIZE=10485760
UPLOAD_STORAGE_PATH=./uploads

# Webhook
WEBHOOK_TIMEOUT=10s
WEBHOOK_MAX_RETRIES=3

# 定时任务
CRON_TOKEN=your-cron-secret-token

# 汇率
EXCHANGE_RATE_PROVIDER=ecb
EXCHANGE_RATE_API_KEY=

# 邮件
MAIL_DRIVER=smtp
MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_FROM_ADDRESS=noreply@example.com
MAIL_FROM_NAME=ZeroLife

# Slack
SLACK_WEBHOOK_URL=

# Pushover
PUSHOVER_APP_TOKEN=

# 2FA
2FA_ISSUER=ZeroLife
```

### 配置加载

```go
type Config struct {
    App      AppConfig
    DB       DBConfig
    Redis    RedisConfig
    JWT      JWTConfig
    CORS     CORSConfig
    Log      LogConfig
    Upload   UploadConfig
    Webhook  WebhookConfig
    Cron     CronConfig
    Exchange ExchangeConfig
    Mail     MailConfig
    Slack    SlackConfig
    Pushover PushoverConfig
    TwoFA    TwoFAConfig
}

func Load() (*Config, error) {
    viper.SetConfigFile(".env")
    viper.AutomaticEnv()
    viper.SetEnvKeyReplacer(strings.NewReplacer(".", "_"))

    if err := viper.ReadInConfig(); err != nil {
        return nil, err
    }

    var cfg Config
    if err := viper.Unmarshal(&cfg); err != nil {
        return nil, err
    }

    return &cfg, nil
}
```

## 2. 数据库初始化

### 连接配置

```go
func InitDB(cfg *DBConfig) *gorm.DB {
    dsn := fmt.Sprintf("%s:%s@tcp(%s:%d)/%s?charset=utf8mb4&parseTime=True&loc=Local",
        cfg.Username, cfg.Password, cfg.Host, cfg.Port, cfg.Database)

    db, err := gorm.Open(mysql.Open(dsn), &gorm.Config{
        Logger: gormlogger.Default.LogMode(gormlogger.Info),
        NamingStrategy: schema.NamingStrategy{
            SingularTable: true,
        },
    })
    if err != nil {
        log.Fatal("数据库连接失败:", err)
    }

    sqlDB, _ := db.DB()
    sqlDB.SetMaxIdleConns(cfg.MaxIdleConns)
    sqlDB.SetMaxOpenConns(cfg.MaxOpenConns)
    sqlDB.SetConnMaxLifetime(time.Duration(cfg.ConnMaxLifetime) * time.Second)

    return db
}
```

### 自动迁移

```go
func AutoMigrate(db *gorm.DB) error {
    return db.AutoMigrate(
        // 用户与权限
        &model.User{},
        &model.UserGroup{},
        &model.GroupMembership{},
        &model.Role{},
        &model.UserRole{},
        &model.InvitedUser{},
        &model.Preference{},

        // 币种
        &model.TransactionCurrency{},
        &model.CurrencyExchangeRate{},

        // 账户
        &model.Account{},
        &model.AccountMeta{},

        // 交易
        &model.TransactionGroup{},
        &model.TransactionJournal{},
        &model.Transaction{},
        &model.JournalMeta{},

        // 分类与标签
        &model.Category{},
        &model.Tag{},

        // 预算
        &model.Budget{},
        &model.BudgetLimit{},
        &model.AvailableBudget{},

        // 储蓄罐
        &model.PiggyBank{},
        &model.PiggyBankEvent{},

        // 账单
        &model.Bill{},

        // 规则
        &model.RuleGroup{},
        &model.Rule{},
        &model.RuleTrigger{},
        &model.RuleAction{},

        // 定期交易
        &model.Recurrence{},
        &model.RecurrenceRepetition{},
        &model.RecurrenceTransaction{},
        &model.RecurrenceTransactionMeta{},
        &model.RecurrenceMeta{},

        // 通用
        &model.Note{},
        &model.Attachment{},
        &model.Location{},
        &model.ObjectGroup{},
        &model.AuditLogEntry{},
        &model.PeriodStatistic{},
        &model.LinkType{},
        &model.TransactionJournalLink{},

        // Webhook
        &model.Webhook{},
        &model.WebhookMessage{},
        &model.WebhookAttempt{},
    )
}
```

### 初始数据

```go
func SeedData(db *gorm.DB) error {
    // 1. 创建默认币种（系统内置28种货币）
    currencies := []model.TransactionCurrency{
        {Name: "人民币", Code: "CNY", Symbol: "¥", DecimalPlaces: 2, Enabled: true, IsDefault: true},
        {Name: "美元", Code: "USD", Symbol: "$", DecimalPlaces: 2, Enabled: true},
        {Name: "欧元", Code: "EUR", Symbol: "€", DecimalPlaces: 2, Enabled: true},
        {Name: "英镑", Code: "GBP", Symbol: "£", DecimalPlaces: 2, Enabled: true},
        {Name: "日元", Code: "JPY", Symbol: "¥", DecimalPlaces: 0, Enabled: true},
        {Name: "瑞士法郎", Code: "CHF", Symbol: "CHF", DecimalPlaces: 2, Enabled: true},
        {Name: "加拿大元", Code: "CAD", Symbol: "C$", DecimalPlaces: 2, Enabled: true},
        {Name: "澳大利亚元", Code: "AUD", Symbol: "A$", DecimalPlaces: 2, Enabled: true},
        {Name: "新西兰元", Code: "NZD", Symbol: "NZ$", DecimalPlaces: 2, Enabled: true},
        {Name: "港币", Code: "HKD", Symbol: "HK$", DecimalPlaces: 2, Enabled: true},
        {Name: "新加坡元", Code: "SGD", Symbol: "S$", DecimalPlaces: 2, Enabled: true},
        {Name: "韩元", Code: "KRW", Symbol: "₩", DecimalPlaces: 0, Enabled: true},
        {Name: "印度卢比", Code: "INR", Symbol: "₹", DecimalPlaces: 2, Enabled: true},
        {Name: "以色列新谢克尔", Code: "ILS", Symbol: "₪", DecimalPlaces: 2, Enabled: true},
        {Name: "俄罗斯卢布", Code: "RUB", Symbol: "₽", DecimalPlaces: 2, Enabled: true},
        {Name: "巴西雷亚尔", Code: "BRL", Symbol: "R$", DecimalPlaces: 2, Enabled: true},
        {Name: "墨西哥比索", Code: "MXN", Symbol: "MX$", DecimalPlaces: 2, Enabled: true},
        {Name: "南非兰特", Code: "ZAR", Symbol: "R", DecimalPlaces: 2, Enabled: true},
        {Name: "土耳其里拉", Code: "TRY", Symbol: "₺", DecimalPlaces: 2, Enabled: true},
        {Name: "泰铢", Code: "THB", Symbol: "฿", DecimalPlaces: 2, Enabled: true},
        {Name: "印尼盾", Code: "IDR", Symbol: "Rp", DecimalPlaces: 0, Enabled: true},
        {Name: "马来西亚林吉特", Code: "MYR", Symbol: "RM", DecimalPlaces: 2, Enabled: true},
        {Name: "菲律宾比索", Code: "PHP", Symbol: "₱", DecimalPlaces: 2, Enabled: true},
        {Name: "波兰兹罗提", Code: "PLN", Symbol: "zł", DecimalPlaces: 2, Enabled: true},
        {Name: "瑞典克朗", Code: "SEK", Symbol: "kr", DecimalPlaces: 2, Enabled: true},
        {Name: "挪威克朗", Code: "NOK", Symbol: "kr", DecimalPlaces: 2, Enabled: true},
        {Name: "丹麦克朗", Code: "DKK", Symbol: "kr", DecimalPlaces: 2, Enabled: true},
        {Name: "匈牙利福林", Code: "HUF", Symbol: "Ft", DecimalPlaces: 0, Enabled: true},
        {Name: "捷克克朗", Code: "CZK", Symbol: "Kč", DecimalPlaces: 2, Enabled: true},
    }
    db.Clauses(clause.OnConflict{DoNothing: true}).Create(&currencies)

    // 2. 创建默认链接类型
    linkTypes := []model.LinkType{
        {Name: "Related", Outward: "相关", Inward: "相关", Editable: false},
        {Name: "Refund", Outward: "退款", Inward: "被退款", Editable: false},
        {Name: "Paid", Outward: "支付", Inward: "被支付", Editable: false},
        {Name: "Reimbursement", Outward: "报销", Inward: "被报销", Editable: false},
    }
    db.Clauses(clause.OnConflict{DoNothing: true}).Create(&linkTypes)

    return nil
}
```

## 3. 应用入口

```go
func main() {
    // 1. 加载配置
    cfg, err := config.Load()
    if err != nil {
        log.Fatal("配置加载失败:", err)
    }

    // 2. 初始化日志
    logger := initLogger(cfg.Log)

    // 3. 初始化数据库
    db := initDB(cfg.DB)
    if err := AutoMigrate(db); err != nil {
        logger.Fatal("数据库迁移失败", zap.Error(err))
    }
    SeedData(db)

    // 4. 初始化 Redis
    redisClient := initRedis(cfg.Redis)

    // 5. 初始化服务层
    services := initServices(db, redisClient, cfg, logger)

    // 6. 初始化 Handler
    handlers := initHandlers(services)

    // 7. 注册路由
    router := SetupRouter(cfg, handlers...)

    // 8. 启动定时任务
    cron := SetupCronJobs(services.Recurrence, services.Bill, services.Currency)
    cron.Start()
    defer cron.Stop()

    // 9. 启动 HTTP 服务
    srv := &http.Server{
        Addr:         fmt.Sprintf(":%d", cfg.App.Port),
        Handler:      router,
        ReadTimeout:  10 * time.Second,
        WriteTimeout: 30 * time.Second,
        IdleTimeout:  60 * time.Second,
    }

    logger.Info("服务启动", zap.Int("port", cfg.App.Port))

    // 优雅关闭
    go func() {
        if err := srv.ListenAndServe(); err != nil && err != http.ErrServerClosed {
            logger.Fatal("服务启动失败", zap.Error(err))
        }
    }()

    quit := make(chan os.Signal, 1)
    signal.Notify(quit, syscall.SIGINT, syscall.SIGTERM)
    <-quit

    logger.Info("服务关闭中...")
    ctx, cancel := context.WithTimeout(context.Background(), 30*time.Second)
    defer cancel()

    if err := srv.Shutdown(ctx); err != nil {
        logger.Fatal("服务关闭失败", zap.Error(err))
    }

    logger.Info("服务已关闭")
}
```

## 4. Docker 部署

### Dockerfile

```dockerfile
# 构建阶段
FROM golang:1.22-alpine AS builder

WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download

COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /app/server ./cmd/server

# 运行阶段
FROM alpine:3.19

RUN apk --no-cache add ca-certificates tzdata
WORKDIR /app

COPY --from=builder /app/server .
COPY --from=builder /app/.env.example .env

EXPOSE 8080

CMD ["./server"]
```

### docker-compose.yml

```yaml
version: '3.8'

services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      - APP_ENV=production
      - DB_HOST=mysql
      - DB_PORT=3306
      - DB_DATABASE=zero_life
      - DB_USERNAME=zero_life
      - DB_PASSWORD=secret
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root_secret
      MYSQL_DATABASE: zero_life
      MYSQL_USER: zero_life
      MYSQL_PASSWORD: secret
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  mysql_data:
  redis_data:
```

## 5. Makefile

```makefile
.PHONY: build run test migrate seed clean

# 构建
build:
	go build -o bin/server ./cmd/server

# 运行
run:
	go run ./cmd/server

# 测试
test:
	go test -v -cover ./...

# 代码检查
lint:
	golangci-lint run

# Swagger 文档生成
swagger:
	swag init -g cmd/server/main.go -o docs

# 数据库迁移
migrate:
	go run ./cmd/server -migrate

# 初始数据
seed:
	go run ./cmd/server -seed

# 清理
clean:
	rm -rf bin/

# Docker 构建
docker-build:
	docker build -t zero-life-api .

# Docker 启动
docker-up:
	docker-compose up -d

# Docker 停止
docker-down:
	docker-compose down
```

## 6. 错误处理规范

### 统一错误类型

```go
type AppError struct {
    Code    string `json:"code"`
    Message string `json:"message"`
    Details []FieldError `json:"details,omitempty"`
    Status  int `json:"-"`
}

type FieldError struct {
    Field   string `json:"field"`
    Message string `json:"message"`
}

// 预定义错误
var (
    ErrUnauthorized    = &AppError{Code: "UNAUTHORIZED", Message: "未认证", Status: 401}
    ErrForbidden      = &AppError{Code: "FORBIDDEN", Message: "权限不足", Status: 403}
    ErrNotFound       = &AppError{Code: "NOT_FOUND", Message: "资源不存在", Status: 404}
    ErrValidation     = &AppError{Code: "VALIDATION_ERROR", Message: "参数校验失败", Status: 400}
    ErrDuplicateEntry = &AppError{Code: "DUPLICATE_ENTRY", Message: "资源已存在", Status: 409}
    ErrInternalServer = &AppError{Code: "INTERNAL_ERROR", Message: "服务器内部错误", Status: 500}
)
```

### 统一响应处理

```go
// Success 成功响应
func Success(c *gin.Context, data interface{}) {
    c.JSON(200, gin.H{"data": data})
}

// Created 创建成功响应
func Created(c *gin.Context, data interface{}) {
    c.JSON(201, gin.H{"data": data})
}

// Paginated 分页响应
func Paginated(c *gin.Context, data interface{}, meta *PaginationMeta, links *PaginationLinks) {
    c.JSON(200, gin.H{
        "data":  data,
        "meta":  meta,
        "links": links,
    })
}

// Error 错误响应
func Error(c *gin.Context, err *AppError) {
    c.JSON(err.Status, gin.H{"error": err})
}
```
